package com.snook.model.objects.core;

public enum ObjectState {
        DEAD,
        STOPPED, RUNNING,
        MOVING, WAITING,
        HEAVY_BRAKE, LIGHT_BRAKE, BRAKE,
        IDLE, BUSY,
        DECELERATING, ACCELERATING,

}
